Mapping between [[Metric Space|metric spaces]] that preserves distances (angles, lengths, and curvature).
Let $(X,d_X)$ and $(Y,d_Y)$ be metric spaces, then $f:X\rightarrow Y$ is an isometry if:
$$
d_Y(f(x_1), f(x_2)) = d_X(x_1,x_2)
$$
for all $x_1,x_2\in X$.
